You or somebody else from <%= @ip %> has requested a new password for the account: <%= @user.username %>

If you did not request a new password then you can simply ignore this email, else follow this link and you will receive a new password right away.

<%= recover_password_url(:id => @user.id, :key => @user.recovery_key) %>